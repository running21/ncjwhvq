apiVersion: v1
kind: Service
metadata:
  name: ncjwhvq
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ncjwhvq"
    port: 8080
  selector:
    app: ncjwhvq
